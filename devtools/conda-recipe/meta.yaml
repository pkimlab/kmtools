{% set version = "0.0.4" %}

package:
  name: ascommon
  version: {{ version }}

source:
  path: ../../

build:
  number: 0

requirements:
  build:
    # Basic python
    - python
  run:
    # Basic python
    - python
    - pyyaml
    - requests
    - lxml
    - pycurl
    - six
    # Scientific python
    - numpy
    - scipy
    - pandas
    - scikit-learn
    - matplotlib
    - pytables
    # 
    - biopython
    - retrying

test:
  requires:
    - pytest
    - pytest-runner
    - pytest-cov
  imports:
    - ascommon

about:
  home: http://ostrokach.github.io/ascommon
  license: MIT
  summary: Helpful bioinformatics scripts.
