{% set version = "0.0.1" %}

package:
  name: ascommon
  version: {{ version }}

source:
  path: ../../

build:
  number: 0

requirements:
  build:
    # Basic python
    - python
    - pyyaml
    - requests
    - lxml
    - pycurl
    # Scientific python
    - numpy
    - scipy
    - pandas
    - scikit-learn
    - biopython
    - retrying
    # Testing
    - pytest
    - pytest-runner
    - pytest-cov
  run:
    # Basic python
    - python
    - pyyaml
    - requests
    - lxml
    - pycurl
    # Scientific python
    - numpy
    - scipy
    - pandas
    - scikit-learn
    - biopython
    - retrying
    # Testing
    - pytest
    - pytest-runner
    - pytest-cov

test:
  imports:
    - ascommon

about:
  home: http://ostrokach.github.io
  license: MIT
  summary: Helpful bioinformatics scripts.

